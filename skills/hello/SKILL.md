---
description: Sanity check that the basejb/skills plugin loaded correctly. Invoked manually to verify install.
---

# Hello

Greet the user warmly in Korean. Confirm that the `basejb` plugin is installed and working. Mention the plugin version from `.claude-plugin/plugin.json` if you can read it, and remind the user that all their personal skills will appear under the `/basejb:` namespace.

Keep it to two short sentences.
